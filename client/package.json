{
  "name": "client",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "watchify ./src/main.js -o ./src/bundle.js -v",
    "watch-producer": "watchify ./src/producer.js -o ./src/bundle-producer.js -v",
    "watch-consumer": "watchify ./src/consumer.js -o ./src/bundle-consumer.js -v",
    "start" : "live-server --https=./https.conf.js ./src"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "browserify": "^17.0.1",
    "live-server": "^1.2.2"
  },
  "dependencies": {
    "mediasoup-client": "^3.7.17",
    "uuid": "^10.0.0"
  }
}
